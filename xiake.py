from hoshino import Service

@sv.on_prefix("开启活动提醒")
async def remind_enable_hatsune(bot, ev):
    gid = str(ev['group_id'])

@sv.on_prefix("关闭活动提醒")
async def remind_enable_hatsune(bot, ev):
    gid = str(ev['group_id'])

@sv.on_prefix("开启卡池提醒")
async def remind_enable_hatsune(bot, ev):
    gid = str(ev['group_id'])

@sv.on_prefix("关闭活动提醒")
async def remind_enable_hatsune(bot, ev):
    gid = str(ev['group_id'])
